{
  "name": "hikconnect-openapi-demo",
  "version": "1.0.0",
  "description": "Demo interactivo para la API de HikConnect OpenAPI (Hik-Connect for Teams)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "deploy": "wrangler deploy",
    "cf-dev": "wrangler dev"
  },
  "engines": {
    "node": ">=22.0.0"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.0",
    "wrangler": "^3.0.0"
  }
}
